<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
	<p>You are logged in as <%= user %>.</p>
	<p>You have <%= cart %> items in your cart.</p>
	
	<form action="addToCartTEST" method="get">	
		<% for(var i=0; i < list.length; i++) { %>
			<input type="radio" name="item" value="<%= {name:list[i].name, seller:list[i].seller} %>"><%= list[i] %>
		<% } %>
    <input id="submit" type="submit" onclick="sendBack()" value="Add to cart">
    </form>
	
  </body>

  <footer>
      <script>
          function sendBack(){
              var test = list;
              alert(list);
              $.ajax({
                  type: "POST",
                  url: "/addToCartTEST",
                  contentType: "application/json",
                  data: JSON.stringify(),
                  success: function(res){
                      alert("working");
                  },
                  error: function(){
                      alert("not so much");
                  }
              });
          }
      </script>
  </footer>
</html>
